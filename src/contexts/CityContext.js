//Abstracción de la creacion del contecto para la data de la ciudad	
import { createContext } from "react";

export const CityContext = createContext();
export const CityProvider = CityContext.Provider; 